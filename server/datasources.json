{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mongodb": {
    "host": "${MONGODB_HOST}",
    "port": "${MONGODB_PORT}",
    "url": "${MONGODB_URL}",
    "database": "${MONGODB_DATABASE}",
    "password": "${MONGODB_PASSWORD}",
    "name": "mongodb",
    "user": "${MONGODB_USER}",
    "connector": "mongodb"
  }
}
